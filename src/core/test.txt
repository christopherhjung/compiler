{
	function lesen(){
		var text = new form.ein.value();
		
		text = text + " ";
		
		form.aus.value = "";
		
		for( int i = 0 , i + 20 , ++i ){
			a=++a		
		}
		
		
		wort = "";
		n = text.indexOf(" ");
		
		wort = text.substring(0,n);
		
		innen = wort.substring(1,n-1);
		
		form.aus.value = wort.charAt(0) + vertausche(innen) + wort.charAt(n-1) + " ";
		int t = ((a,b)->x+b)(1,2)
	}



}